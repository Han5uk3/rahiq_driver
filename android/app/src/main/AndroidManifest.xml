<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- supportsRtl is off by default, which leaves every native screen
         resolving start/end as left/right even on an Arabic device. Flutter
         draws its own layouts and never noticed, but the Freshchat chat is a
         native Activity: its RTL margins landed on the wrong side and pushed
         the whole conversation into a narrow column with dead space beside
         it. Turning it on lets those screens mirror properly. -->
    <application
        android:label="Rahiq Driver"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true"
        android:supportsRtl="true">
        <!-- taskAffinity must match the applicationId, not be empty. Freshchat's
             chat screens are native Activities declared in its own library
             manifest with no affinity of their own, so they inherit the default
             one — the package name. An empty affinity here put MainActivity in
             a different task family, so launching the chat spawned a *second*
             task: the SDK view showed up in recents as a separate copy of the
             app, with Back leaving the app rather than returning to it. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity="com.rahiq.driver"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <meta-data android:name="com.google.android.geo.API_KEY"
android:value="AIzaSyB___mbW9IyVrmRpxFN1sYXwPjtZVhZ-x0"/>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel_v2" />

        <!-- Freshchat hands the camera app a content:// URI to write the capture
             into, which needs a FileProvider the app declares — the SDK's own
             AAR deliberately does not, so that the authority stays unique to
             whichever app embeds it. Without this the SDK logs
             "Missing/Bad FileProvider for Freshchat" at startup and camera
             attachments fail on Android 7 and later.

             The authority must also be repeated in @string/freshchat_file_provider_authority,
             which is how the SDK looks it up; the paths file is the SDK's own,
             merged in from its AAR, and already covers the directories it writes
             to. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/freshchat_file_provider_paths" />
        </provider>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
